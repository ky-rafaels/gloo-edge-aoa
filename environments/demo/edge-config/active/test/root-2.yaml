apiVersion: gateway.solo.io/v1
kind: RouteTable
metadata:
  name: 'root-routes'
  namespace: 'gloo-system'
spec:
  weight: 20
  routes:
  - matchers:
    - prefix: /admin/fleetcard
    name: admin-fleetcard-fooname
    routeAction:
      single:
        upstream:
          name: 'default-httpbin-8000'
    options:
        prefixRewrite: '/'
  - matchers:
    - regex: /v[0-9]+/.*
    name: root-regex-barname
    routeAction:
      single:
        upstream:
          name: 'default-httpbin-8000'
    options:
        prefixRewrite: '/'
  